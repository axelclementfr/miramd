<script lang="ts">
  /**
   * Editor.svelte — Drop-in wrapper that delegates to the modular EditorContainer.
   * Kept at the original path so all existing imports continue to work.
   */
  import EditorContainer from './editor/EditorContainer.svelte';
</script>

<EditorContainer />
